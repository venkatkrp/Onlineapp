cd <move to working directory>
git clone https://github.com/venkatkrp/Onlineapp.git
git version
git config --global user.name "venkatkrp"
git config --global user.email "venkatkrp@gmail.com"
git config --list 
git help <verb>
git init
git remote add origin https://github.com/venkatkrp/Onlineapp.git
git add -A (or specfic file)
git status
git commit -m "message"

git branch branch(new branch)

git checkout branch

git add
git commit -m
git -u orgin <branchname>
git pull origin master

--delete branch

git branch --merged
git merge branchname
git push origin mastger

git push origin --delete <branch name>