Git 

cd /
cd /d
ls
git config --global user.name ""
git config --global user.email ""
get config --list

git add 
git status
git -u orgin master


git remote add origin "https://github.com/venkatkrp/Proj1.git"

git clone -b <branch> <remote_repo>

git branch branch(new branch)

git checkout branch

git add
git commit -m
git -u orgin <branchname>
git pull origin master

--delete branch

git branch --merged
git merge branchname
git push origin mastger

git push origin --delete <branch name>